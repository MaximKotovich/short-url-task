
### Steps to run application
``` 
1. Open folder with frontend project in terminal
2. Create image - run 'sudo docker build -t frontend .' to terminal
3. Run docker container - run 'docker run -dp 3000:3000 --name frontend-container frontend:latest' in terminal
4. paste link 'http://localhost:3000/' to your browser 
```